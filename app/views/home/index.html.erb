<%= render 'shared/form.html.erb' %>

<p>登録順(昇順) | 降順 | 月別 | アルファベット順 | 日本語 | COMPLETE</p>

<ul id="words">
<% @words.each do |word| %>
  <li class="word">
    <div class="english">
      <%= word.english %>
    </div>
    <div class="japanese" style="display:none;">
      <%= word.japanese %>
    </div>
  </li>
<% end %>
</ul>
